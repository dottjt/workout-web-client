export default function Workout() {
  return (
    <div>
      <h1>Workout</h1>
      <div>
        {/* {workoutList.map(workout => {
          <p>{workout.name}</p>
        })} */}
      </div>

      <button onClick={}>Add workout</button>
    </div>
  );
}
